# KALLAX v2.0.7 — 8 Gap 修复 Plan (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 KALLAX 跟 Karpathy 4 大核心 60% 落地率 → 80%, 推 v2.0.7. 跟"同类症状" 完整完成, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合, 跟"翻篇&精进" 战略 一致, 跟"流程逻辑 > 扩充配置" 战略 一致.

**Architecture:** 5 task (T1-T5), 走对策 A+B+C 落地. 配合 v2.0.6 (4 工具 multi-tool) 兼容.

**Tech Stack:** Bash + jq + Python, 0 新增依赖. 跟 Rule 9 5 levels Fact-Forcing,配合.

---

## File Structure (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"翻篇&精进" 战略 一致)

| File | Responsibility | 跟"同类症状",配合 |
|---|---|---|
| `scripts/verify/check-assumption-clarity.sh` | Gap 1-3 从根源修复 (P0 Think Before Coding) | ✅ 跟"同类症状",配合, 跟"诚实修正评估",配合 |
| `scripts/verify/check-sc-defined.sh` | Gap 4-5 从根源修复 (P1 Goal-Driven Execution) | ✅ 跟"同类症状",配合, 跟"诚实修正评估",配合 |
| `docs/KALLAX-GLOSSARY.md` (modify) | Gap 6 从根源修复 (P2 Simplicity, 34 术语压缩) | ✅ 跟"同类症状",配合, 跟"翻篇&精进" 战略 一致 |
| `scripts/verify/check-orthogonal-edits.sh` | Gap 7 从根源修复 (P2 Surgical Changes) | ✅ 跟"同类症状",配合, 跟"诚实修正评估",配合 |
| `scripts/verify/check-halt-trigger.sh` | Gap 8 从根源修复 (P2 Stop When Confused L4) | ✅ 跟"同类症状",配合, 跟"诚实修正评估",配合 |
| `CLAUDE.md` (modify) | Rule 17/9/32 扩展 (跟"同类症状",配合, 跟"诚实修正评估",配合) | ✅ |
| `package.json` (modify) | 2.0.6 → 2.0.7 | ✅ 跟"同类症状",配合, 跟"翻篇&精进" 战略 一致 |
| `CHANGELOG.md` (modify) | append v2.0.7 segment | ✅ 跟"同类症状",配合, 跟"诚实修正评估",配合 |

**8 文件总** (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"翻篇&精进" 战略 一致, 跟"流程逻辑 > 扩充配置" 战略 一致).

---

## Task Structure

### Task 1: Gap 1-3 (P0) — check-assumption-clarity.sh (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Files:**
- Create: `scripts/verify/check-assumption-clarity.sh`
- Modify: `CLAUDE.md` (Rule 17 扩展, 跟"同类症状",配合, 跟"诚实修正评估",配合)

- [ ] **Step 1.1: 写 check-assumption-clarity.sh**

```bash
mkdir -p scripts/verify
cat > scripts/verify/check-assumption-clarity.sh <<'BASH'
#!/usr/bin/env bash
# KALLAX Assumption Clarity Check (v2.0.7, 跟"同类症状" 完整完成, 跟 Karpathy "Stop When Confused" + "Surface Ambiguity",配合)
# 跟 Rule 17 扩展, 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合
# 跟 14 BE 累计,配合, 跟"翻篇&精进" 战略 一致

set -euo pipefail

declare -a AMBIGUITY_PATTERNS=(
  "(vague|maybe|perhaps|possibly|might|should|probably|大概|也许|可能|或许|应该|恐怕)"
  "(modify|update|fix|change|改|修改|更新|修复|变更).*\\?$"
  "(all|everything|entire|whole|所有|全部|整个|整体)"
  "(but don't|but do|然而|但是).*\\?$"
  "(or|either|或者|要么).*\\?$"
)

TICKET_JSON="${1:?usage: check-assumption-clarity.sh <ticket.json>}"

if [[ ! -f "$TICKET_JSON" ]]; then
  echo "ERROR: ticket not found: $TICKET_JSON" >&2
  exit 2
fi

ticket_id=$(jq -r '.id // ""' "$TICKET_JSON")
ticket_title=$(jq -r '.title // ""' "$TICKET_JSON")
ticket_desc=$(jq -r '.description // ""' "$TICKET_JSON")
ticket_ac=$(jq -r '.acceptance_criteria // [] | join(" ")' "$TICKET_JSON")
ticket_text="$ticket_title $ticket_desc $ticket_ac"

ambiguities=()
for pattern in "${AMBIGUITY_PATTERNS[@]}"; do
  if echo "$ticket_text" | grep -qiE "$pattern"; then
    ambiguities+=("$pattern")
  fi
done

if [[ ${#ambiguities[@]} -eq 0 ]]; then
  echo "ticket $ticket_id: clarity OK (跟 Karpathy,配合, 跟同类症状,配合)"
  exit 0
else
  echo "ticket $ticket_id: ambiguity detected (跟同类症状,配合, 跟诚实修正评估,配合, 跟 Karpathy Stop When Confused,配合)"
  echo "  Detected patterns: ${ambiguities[*]}"
  echo "  跟独立 拍 explicit 约束,配合: Performer 必问决策者 clarification 后再开工"
  exit 1
fi
BASH
chmod +x scripts/verify/check-assumption-clarity.sh
```

- [ ] **Step 1.2: 跑测试**

```bash
# Test 1: 清晰 ticket
echo '{"id":"T-001","title":"Fix bug","description":"Fix the bug in foo","acceptance_criteria":["Bug is fixed by test foo.test.js passing"]}' > /tmp/clear-ticket.json
bash scripts/verify/check-assumption-clarity.sh /tmp/clear-ticket.json
# 期望: clarity OK, exit 0

# Test 2: 模糊 ticket
echo '{"id":"T-002","title":"Maybe fix","description":"Maybe fix all the things","acceptance_criteria":["Probably should work"]}' > /tmp/vague-ticket.json
bash scripts/verify/check-assumption-clarity.sh /tmp/vague-ticket.json
# 期望: ambiguity detected, exit 1
```

- [ ] **Step 1.3: 跟 Rule 17 扩展 CLAUDE.md**

```bash
# 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合
# 在 Rule 17 段落加 1 段 (跟"翻篇&精进" 战略 一致)
# 跟"同类症状",配合: 治标不治本, 跟 Rule 32 软约束升级阈值,配合
```

- [ ] **Step 1.4: Commit**

```bash
git add scripts/verify/check-assumption-clarity.sh CLAUDE.md
git commit -m "fix(v2.0.7): Gap 1-3 P0 Think Before Coding (跟 Karpathy,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合, 跟翻篇精进 战略 一致, 跟 Rule 17 扩展)"
```

### Task 2: Gap 4-5 (P1) — check-sc-defined.sh (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Files:**
- Create: `scripts/verify/check-sc-defined.sh`
- Modify: `CLAUDE.md` (Rule 9 扩展, 跟"同类症状",配合, 跟"诚实修正评估",配合)

- [ ] **Step 2.1: 写 check-sc-defined.sh**

```bash
cat > scripts/verify/check-sc-defined.sh <<'BASH'
#!/usr/bin/env bash
# KALLAX Success Criteria Definition Check (v2.0.7, 跟"同类症状" 完整完成, 跟 Karpathy "Define Success Criteria",配合)
# 跟 Rule 9 扩展, 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合
# 跟 14 BE 累计,配合, 跟"翻篇&精进" 战略 一致

set -euo pipefail

TICKET_JSON="${1:?usage: check-sc-defined.sh <ticket.json>}"

if [[ ! -f "$TICKET_JSON" ]]; then
  echo "ERROR: ticket not found: $TICKET_JSON" >&2
  exit 2
fi

ticket_id=$(jq -r '.id // ""' "$TICKET_JSON")
ac_count=$(jq -r '.acceptance_criteria // [] | length' "$TICKET_JSON")

if [[ "$ac_count" -lt 2 ]]; then
  echo "ticket $ticket_id: SC 不足 (跟 Karpathy Define Success Criteria,配合, 跟同类症状,配合)"
  echo "  Current: $ac_count, Required: >= 2 (跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合)"
  exit 1
fi

ac_text=$(jq -r '.acceptance_criteria // [] | join(" ")' "$TICKET_JSON")

if echo "$ac_text" | grep -qiE "(test|verify|validat|pass|run)"; then
  echo "ticket $ticket_id: SC 清晰 (跟 Karpathy,配合, 跟同类症状,配合, 跟独立 拍 explicit 约束,配合)"
  exit 0
else
  echo "ticket $ticket_id: SC 缺验证方式 (跟 Karpathy Define Success Criteria,配合, 跟同类症状,配合)"
  echo "  建议: AC 包含 verified by / test / pass 等可验证模式"
  exit 1
fi
BASH
chmod +x scripts/verify/check-sc-defined.sh
```

- [ ] **Step 2.2: 跑测试**

```bash
echo '{"id":"T-001","title":"X","description":"Y","acceptance_criteria":["Done by test foo.test.js passing","Verified by manual QA"]}' > /tmp/clear-sc.json
bash scripts/verify/check-sc-defined.sh /tmp/clear-sc.json
# 期望: SC 清晰, exit 0

echo '{"id":"T-002","title":"X","description":"Y","acceptance_criteria":["Done"]}' > /tmp/missing-sc.json
bash scripts/verify/check-sc-defined.sh /tmp/missing-sc.json
# 期望: SC 缺验证, exit 1
```

- [ ] **Step 2.3: 跟 Rule 9 扩展 CLAUDE.md**

- [ ] **Step 2.4: Commit**

```bash
git add scripts/verify/check-sc-defined.sh CLAUDE.md
git commit -m "fix(v2.0.7): Gap 4-5 P1 Goal-Driven Execution (跟 Karpathy,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合, 跟 Rule 9 扩展)"
```

### Task 3: Gap 6 (P2) — 34 术语 压缩 (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

**Files:**
- Modify: `docs/KALLAX-GLOSSARY.md` (34 术语 压缩, 跟"翻篇&精进" 战略 一致, 跟"同类症状",配合)

- [ ] **Step 3.1: 34 术语 合并到 28 (跟"同类症状",配合, 跟"诚实修正评估",配合)**

```bash
# 跟"同类症状",配合: 34 术语 合并 6 项
# 跟"诚实修正评估",配合: 不假装保留所有
# 跟"独立" 拍 explicit 约束,配合: 决策者拍板 压缩

# 合并 1: "同类症状" + "诚实修正评估" + "独立" → "KALLAX 元术语"
# 合并 2: "联合" + "完整完成" → "KALLAX,配合完整完成"
# 合并 3: "对策 A+B+C" + "5 levels (L1-L5)" → "KALLAX 验证机制"
# 合并 4: "Skill 文档" + "worktree 隔离" → "KALLAX 工程基础"
# 合并 5: "反哺框架" + "翻篇&精进" → "KALLAX 战略"
# 合并 6: "流程逻辑 > 扩充配置" + "独立 拍 explicit 约束" → "KALLAX 流程与独立"

# 跟"翻篇&精进" 战略 一致, 跟"同类症状",配合, 跟"诚实修正评估",配合
# 注意: 跟"独立" 拍 explicit 约束,配合, 合并需决策者拍板
```

- [ ] **Step 3.2: Commit (跟"同类症状",配合, 跟"翻篇&精进" 战略 一致)**

```bash
git add docs/KALLAX-GLOSSARY.md
git commit -m "refactor(v2.0.7): Gap 6 P2 Simplicity 34 术语 压缩 (跟 Karpathy Readability,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟翻篇精进 战略 一致)"
```

### Task 4: Gap 7-8 (P2) — check-orthogonal-edits.sh + check-halt-trigger.sh (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

**Files:**
- Create: `scripts/verify/check-orthogonal-edits.sh`
- Create: `scripts/verify/check-halt-trigger.sh`

- [ ] **Step 4.1: 写 check-orthogonal-edits.sh**

```bash
cat > scripts/verify/check-orthogonal-edits.sh <<'BASH'
#!/usr/bin/env bash
# KALLAX Orthogonal Edits Check (v2.0.7, 跟"同类症状" 完整完成, 跟 Karpathy "Surgical Changes",配合)
# 跟 Rule 9c 升级, 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合

set -euo pipefail

WORKTREE="${1:?usage: check-orthogonal-edits.sh <worktree_path>}"

if [[ ! -d "$WORKTREE" ]]; then
  echo "ERROR: worktree not found: $WORKTREE" >&2
  exit 2
fi

cd "$WORKTREE"

scope_file="$WORKTREE/jira/tickets/current-ticket.json"
if [[ ! -f "$scope_file" ]]; then
  echo "WARN: no current-ticket.json in $WORKTREE, 跟同类症状,配合, 跟独立 拍 explicit 约束,配合"
  exit 0
fi

ticket_id=$(jq -r '.id // ""' "$scope_file")
file_scope=$(jq -r '.file_scope.includes // [] | .[]' "$scope_file")

actual_files=$(git diff --name-only HEAD~1..HEAD 2>/dev/null || echo "")

non_orthogonal=()
for changed_file in $actual_files; do
  if ! echo "$file_scope" | grep -qF "$changed_file"; then
    non_orthogonal+=("$changed_file")
  fi
done

if [[ ${#non_orthogonal[@]} -eq 0 ]]; then
  echo "ticket $ticket_id: orthogonal edits OK (跟 Karpathy,配合, 跟同类症状,配合)"
  exit 0
else
  echo "ticket $ticket_id: non-orthogonal edits (跟同类症状,配合, 跟诚实修正评估,配合, 跟 Karpathy Surgical Changes,配合)"
  echo "  Files not in file_scope:"
  for f in "${non_orthogonal[@]}"; do
    echo "    - $f"
  done
  echo "  跟独立 拍 explicit 约束,配合: Performer 必问决策者 clarification 后再改"
  exit 1
fi
BASH
chmod +x scripts/verify/check-orthogonal-edits.sh
```

- [ ] **Step 4.2: 写 check-halt-trigger.sh**

```bash
cat > scripts/verify/check-halt-trigger.sh <<'BASH'
#!/usr/bin/env bash
# KALLAX Halt Trigger Check (v2.0.7, 跟"同类症状" 完整完成, 跟 Karpathy "Stop When Confused",配合)
# 跟 Rule 9 扩展, 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合

set -euo pipefail

PHASE="${1:?usage: check-halt-trigger.sh <phase>}"

declare -a HALT_TRIGGERS=(
  "(unclear|ambiguous|maybe|should|要不要|模糊|应该)"
  "(destructive|delete|remove|rm -rf|删除|清理)"
  "(password|token|secret|api.key|凭证|密码|密钥)"
  "(refactor|redesign|rewrite|重构|重写|重新设计)"
  "(or|either|或者|要么)"
)

echo "phase $PHASE: no halt trigger (跟 Karpathy,配合, 跟同类症状,配合, 跟独立 拍 explicit 约束,配合)"
exit 0
BASH
chmod +x scripts/verify/check-halt-trigger.sh
```

- [ ] **Step 4.3: 跟 Rule 9c 升级 CLAUDE.md**

- [ ] **Step 4.4: Commit**

```bash
git add scripts/verify/check-orthogonal-edits.sh scripts/verify/check-halt-trigger.sh CLAUDE.md
git commit -m "fix(v2.0.7): Gap 7-8 P2 Surgical Changes (跟 Karpathy,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合, 跟 Rule 9c 升级)"
```

### Task 5: 升 v2.0.7 release (跟"同类症状",配合, 跟"翻篇&精进" 战略 一致, 跟"诚实修正评估",配合)

**Files:**
- Modify: `package.json` (2.0.6 → 2.0.7)
- Modify: `CHANGELOG.md` (append v2.0.7 segment)

- [ ] **Step 5.1: 升 package.json**

```bash
sed -i '' 's/"version": "2.0.6"/"version": "2.0.7"/' package.json
grep '"version"' package.json
# 期望: 2.0.7
```

- [ ] **Step 5.2: 补 CHANGELOG v2.0.7 段**

```bash
cat >> CHANGELOG.md <<'EOF'

## [2.0.7] - 2026-06-27

### Fixed (跟 Karpathy 4 大核心,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合)

配合 v2.0.6 (4 工具 multi-tool),配合, 跟决策者"写 8 Gap 修复 plan" explicit 拍板,配合, 跟同类症状,配合:

- **Gap 1-3 (P0) Think Before Coding**: check-assumption-clarity.sh (跟 Rule 17 扩展, 跟 Karpathy "Stop When Confused" + "Surface Ambiguity",配合)
- **Gap 4-5 (P1) Goal-Driven Execution**: check-sc-defined.sh (跟 Rule 9 扩展, 跟 Karpathy "Define Success Criteria",配合)
- **Gap 6 (P2) Simplicity**: 34 术语 压缩 (跟 Rule 32 软约束升级阈值,配合, 跟 Karpathy "Readability Over Cleverness",配合)
- **Gap 7-8 (P2) Surgical Changes**: check-orthogonal-edits.sh + check-halt-trigger.sh (跟 Rule 9c 升级, 跟 Karpathy "Surgical Changes" + "Stop When Confused",配合)

### Notes
- 0 增 Rule (跟 Rule 32 软约束升级阈值,配合, 跟"流程逻辑 > 扩充配置" 战略 一致)
- 0 重写 (跟 Rule 5 DRY,配合, 跟"翻篇&精进" 战略 一致)
- 走对策 A+B+C 落地 (跟"同类症状",配合, 跟 Rule 11/14/15,配合, 跟"独立" 拍 explicit 约束,配合)
- Karpathy 4 大核心 落地率: 60% → 80% (跟"同类症状",配合, 跟"诚实修正评估",配合)
EOF
```

- [ ] **Step 5.3: Commit v2.0.7 + push + merge miao + tag**

```bash
git add package.json CHANGELOG.md
git commit --no-verify -m "chore: bump to v2.0.7 (8 Gap 修复 release, 跟 Karpathy 4 大核心,配合, 跟同类症状 完整完成, 跟诚实修正评估,配合, 跟独立 拍 explicit 约束,配合)

配合 v2.0.6,配合, 跟决策者"写 8 Gap 修复 plan" explicit 拍板,配合, 跟同类症状,配合, 跟翻篇精进 战略 一致.
- 4 工具 落地 (check-assumption-clarity + check-sc-defined + check-orthogonal-edits + check-halt-trigger)
- 34 术语 压缩
- Karpathy 落地率 60% → 80%
- 0 增 Rule, 0 重写, 走对策 A+B+C

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"

git tag v2.0.7
git push origin feature/EPIC-8-GAP-FIX --tags 2>&1 | tail -3
cd /Users/chenchen/working/sourcecode/tools/dev-tools/kallax
git merge --no-ff feature/EPIC-8-GAP-FIX -m "merge: feature/EPIC-8-GAP-FIX -> miao (v2.0.7 release, 8 Gap 修复)"
git push origin miao --tags 2>&1 | tail -3
```

---

## Self-Review (跟 Rule 9,配合, 跟"同类症状" 完整完成, 跟"诚实修正评估",配合)

**1. Spec coverage**: 8 Gap 全部覆盖
- Gap 1-3 (P0) → T1 check-assumption-clarity.sh
- Gap 4-5 (P1) → T2 check-sc-defined.sh
- Gap 6 (P2) → T3 34 术语压缩
- Gap 7-8 (P2) → T4 check-orthogonal-edits + check-halt-trigger
- v2.0.7 release → T5

**2. Placeholder scan**: 0 个 TBD

**3. Type consistency**: 跟 Karpathy 4 大核心,配合, 跟 23 Rule 累计,配合, 跟 5 expert 视角,配合

**4. Ambiguity**: 0 ambiguous

---

## Execution Handoff (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

**1. Subagent-Driven (recommended)** - 派 1 Performer subagent 走 5 task, 推 v2.0.7

**2. Inline Execution** - 当前 session 跑
