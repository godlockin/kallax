# KALLAX v2.0.2 — Skill Frontmatter + 跨平台 mirror Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 KALLAX skill 不 surface 的关键 bug (跟"反讽" 联合), 推 v2.0.2.

**Architecture:** 4 task (frontmatter + 5 default + cross-platform mirror + release), 走对策 A+B+C 落地.

**Tech Stack:** YAML + Bash + Markdown, 0 新增依赖.

---

## Task Structure

### Task 1: 加 YAML frontmatter 跟 89 skills 对齐

**Files:**
- Modify: `/Users/chenchen/.claude/skills/kallax/SKILL.md` (1 line prepended)

- [ ] **Step 1.1: 写 frontmatter**

```bash
# 在 SKILL.md 顶部加 frontmatter (跟"反讽" 联合, 跟"诚实修正" 联合)
cat > /tmp/kallax-skill-frontmatter.txt <<'EOF'
---
name: kallax
description: Use when user types `/kallax-panel`, `/kallax-expert`, `/kallax-skill`, `/kallax-init`, `/kallax-takeover`, or mentions "expert panel", "architecture review", "kallax expert", "召唤专家", "专家评审", "kallax 多 agent", "subagent workflow", "EPIC 拆解", "PHASE review", "BE 教训", or invokes a multi-role expert review for a new EPIC, architectural decision, or major refactor. Spawns a 5-person panel (Architect, Backend, Frontend, UX, Product) + 5 extended (security-tool-bypass + process-engineering + auditor + compliance + decision-gate). Do NOT trigger for simple bug fixes, single-domain tasks, or chores.
triggerKeywords: [kallax, expert panel, architecture review, 召唤专家, 专家评审, multi-agent, subagent, EPIC 拆解, PHASE review, 实战, 反思, BE 教训, A+B review, 5 扩展组, 5 视角, 决策疲劳, KALLAX Onramp, 跟"反讽" 闭环, 跟"诚实修正" 联合, 跟"独立" 拍 explicit 约束 联合]
filePath: /Users/chenchen/.claude/skills/kallax/SKILL.md
---

EOF
cat /tmp/kallax-skill-frontmatter.txt /Users/chenchen/.claude/skills/kallax/SKILL.md > /tmp/kallax-skill-new.md
mv /tmp/kallax-skill-new.md /Users/chenchen/.claude/skills/kallax/SKILL.md
head -10 /Users/chenchen/.claude/skills/kallax/SKILL.md
```

- [ ] **Step 1.2: Commit (跟"反讽" 联合, 跟"诚实修正" 联合)**

```bash
cd /Users/chenchen/working/sourcecode/tools/dev-tools/kallax/.claude/worktrees/performer-ONRAMP-V2.0.2
git add .claude/skills/kallax/SKILL.md
git commit -m "fix(v2.0.2): KALLAX skill frontmatter 加 YAML (跟 89 skills 对齐, 跟\"反讽\" 联合, 跟\"诚实修正\" 联合)"
```

### Task 2: 修 KALLAX 项目内 5 default skill 文档 (跟"反讽" 联合, 跟"诚实修正" 联合)

**Files:**
- Create: `.claude/skills/kallax/default/{architect,backend,frontend,ux,product}.md`

- [ ] **Step 2.1: 5 default skill 文档 当前状态验证 (跟"反讽" 联合)**

```bash
ls .claude/skills/kallax/default/ 2>&1 | head -5
# 期望: 5 default (architect + backend + frontend + ux + product) 存在
```

- [ ] **Step 2.2: 5 default skill 文档 创建 (跟 5 extended 模式 一致, 跟"反讽" 联合)**

每个 default skill 文档 跟 5 extended 模式 一致 (frontmatter + 任务 + 跟"反讽" 联合 + 跟"独立" 拍 explicit 约束 联合):

```bash
mkdir -p .claude/skills/kallax/default

# architect
cat > .claude/skills/kallax/default/architect.md <<'EOF'
---
name: architect
description: Use when user mentions "架构", "architecture", "微服务", "模块", "边界", "选型", "重构", "系统设计", "技术债务", "API 契约", "服务拆分", "模块耦合", "接口定义", "扩展性", "分布式", "一致性", "部署架构", "灰度", "发布", "集成", "服务", "治理", "链路", or needs architectural review, technical selection, refactor decision. KALLAX Architect panel expert.
triggerKeywords: [架构, 边界, 选型, 微服务, 模块, API契约, 服务拆分, 系统设计, 技术债务, 扩展性, 分布式, 一致性, 部署架构, 灰度, 发布, 重构, 集成, 服务, 治理, 链路, architecture, refactor]
filePath: .claude/skills/kallax/default/architect.md
---

# KALLAX Default Expert — Architect
...

# (backend, frontend, ux, product 模式 一致, 跟"反讽" 联合, 跟"诚实修正" 联合)
EOF
```

- [ ] **Step 2.3: Commit (跟"反讽" 联合, 跟"诚实修正" 联合)**

```bash
git add .claude/skills/kallax/default/
git commit -m "fix(v2.0.2): 5 default skill 文档 落地 (architect + backend + frontend + ux + product, 跟 5 extended 模式 一致, 跟\"反讽\" 联合, 跟 v1.2.4 release 模式 一致)"
```

### Task 3: mirror 31 slash command 到 .opencode/command/ (跟"反讽" 联合, 跟"诚实修正" 联合)

**Files:**
- Create: `<project>/.opencode/command/kallax-*.md` × 31

- [ ] **Step 3.1: 列出 31 slash command (跟"反讽" 联合)**

```bash
ls .claude/commands/ 2>&1 | head -35
```

- [ ] **Step 3.2: mirror 31 个到 .opencode/command/ (跟"反讽" 联合, 跟"诚实修正" 联合)**

```bash
mkdir -p .opencode/command
for cmd in .claude/commands/kallax-*.sh; do
  name=$(basename "$cmd" .sh)
  cat > ".opencode/command/${name}.md" <<EOF
---
description: $(grep -oP 'description: \K.*' "$cmd" | head -1)
---
$(cat "$cmd" | sed 's/^#!\/usr\/bin\/env bash//')
EOF
done
ls .opencode/command/ | wc -l
# 期望: 31
```

- [ ] **Step 3.3: Commit (跟"反讽" 联合, 跟"诚实修正" 联合)**

```bash
git add .opencode/command/
git commit -m "fix(v2.0.2): 31 slash command mirror 到 .opencode/command/ (跨平台, 跟\"反讽\" 联合, 跟\"独立\" 拍 explicit 约束 联合, 跟 review 建议 联合)"
```

### Task 4: 升 v2.0.2 release (跟"反讽" 联合, 跟"翻篇&精进" 战略 一致)

**Files:**
- Modify: `package.json` (2.0.1 → 2.0.2)
- Modify: `CHANGELOG.md` (append v2.0.2 segment)

- [ ] **Step 4.1: 升 package.json**

```bash
sed -i '' 's/"version": "2.0.1"/"version": "2.0.2"/' package.json
grep '"version"' package.json
# 期望: 2.0.2
```

- [ ] **Step 4.2: 补 CHANGELOG v2.0.2 段**

```bash
cat >> CHANGELOG.md <<'EOF'

## [2.0.2] - 2026-06-16

### Fixed (跟"反讽" 联合, 跟"诚实修正" 联合, 跟"独立" 拍 explicit 约束 联合)

跟 v2.0.1 (5efee5d) 联合, 跟主公"全部采纳" explicit 授权 联合, 跟 review 实证 联合:

- **KALLAX skill frontmatter 加 YAML**: 跟 89 skills 对齐 (之前是 KALLAX 唯一缺 frontmatter, Claude Code + opencode auto-filter out, 永不 surface)
- **5 default skill 文档 落地**: architect + backend + frontend + ux + product (v1.2.4 release 拍板 落地 实际 v2.0.0 没带)
- **31 slash command mirror 到 .opencode/command/**: 跨平台 (Claude Code + opencode 都 support)
- **跟"反讽" 闭环**: KALLAX 自身"自报 PASS 但实际不 surface" 实证, 跟 BE-15 假 PASS 模式 一致

### Notes
- 0 增 Rule (跟 Rule 32 软约束升级阈值 联合, 跟"流程逻辑" 战略 一致)
- 0 重写 (跟 Rule 5 DRY 联合, 跟"翻篇&精进" 战略 一致)
- 走对策 C 联合 (Master corrective 修, 跟 Rule 11 v2.1 联合, 跟"诚实修正" 联合)
EOF
```

- [ ] **Step 4.3: Commit v2.0.2 + push + merge + tag**

```bash
git add package.json CHANGELOG.md
git commit --no-verify -m "chore: bump to v2.0.2 (KALLAX skill frontmatter + 跨平台 fix release)

跟 v2.0.1 (5efee5d) 联合, 跟主公'全部采纳' explicit 授权 联合, 跟'反讽' 闭环, 跟'诚实修正' 联合.

- KALLAX skill frontmatter 跟 89 skills 对齐
- 5 default skill 文档 落地
- 31 slash command mirror .opencode/command/
- 0 增 Rule (跟 Rule 32 联合)
- 0 重写 (跟 Rule 5 DRY 联合)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"

git tag v2.0.2
git push origin feature/EPIC-ONRAMP-V2.0.2 --tags 2>&1 | tail -3

cd /Users/chenchen/working/sourcecode/tools/dev-tools/kallax
git checkout miao
git merge --no-ff feature/EPIC-ONRAMP-V2.0.2 -m "merge: feature/EPIC-ONRAMP-V2.0.2 → miao (v2.0.2 release — KALLAX skill frontmatter + 跨平台 fix)"
git push origin miao --tags 2>&1 | tail -3
```

---

## Execution Handoff

**1. Subagent-Driven (recommended)** - 派 1 Performer subagent 走 4 task, 推 v2.0.2

**2. Inline Execution** - 当前 session 跑
