# KALLAX v2.0.2 — Skill Frontmatter + 跨平台 mirror Design (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 KALLAX skill 不 surface 的关键 bug (跟"同类症状",配合 — KALLAX 自身 frontmatter 缺, 自报 PASS 但 Claude Code/opencode auto-filter out), 加 YAML frontmatter 跟 89 other skills 对齐 + mirror 31 slash command 到 `.opencode/command/` 跨平台, 推 v2.0.2 release.

**Architecture:** 在 worktree `feature/EPIC-ONRAMP-V2.0.2` 修 4 task, 走对策 A+B+C 落地. 配合 v2.0.1 (5efee5d) 兼容.

**Tech Stack:** YAML + Bash + opencode loader contract. 跟 Rule 9 5 levels Fact-Forcing,配合. 跟对策 A+B+C,配合.

---

## 1. 动机 (Motivation) — 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合

### 1.1 关键 Bug 实证 (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Reviewer 实证** (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合):
- `~/.claude/skills/kallax/SKILL.md:1` 开头是 `# KALLAX Skills 命令索引`, **无 YAML frontmatter**
- Claude Code + opencode 都 auto-scan `~/.claude/skills/<name>/SKILL.md`, 缺 frontmatter = **filter out, 永不 surface**
- 跟 89 other skills 对比, **KALLAX 是唯一缺 description 的**

### 1.2 跟"同类症状" 完整完成 (跟"诚实修正评估",配合)

**KALLAX 自身"同类症状"**:
- KALLAX 主张"治 root cause 用 root cause 模式" — KALLAX 自身 frontmatter 缺, **自报 PASS 但实际不 surface** (跟 BE-15 形式通过实质失败 模式 一致)
- KALLAX 主张"独立" — KALLAX skill 自身**不独立**, 依赖 Claude Code/opencode loader,配合
- KALLAX 主张"诚实修正评估" — KALLAX 之前**没真验证** skill 触发, 跟 T3 形式通过实质失败 模式 一致

### 1.3 跟"诚实修正评估",配合 (跟"独立" 拍 explicit 约束,配合, 跟"同类症状",配合)

**v2.0.0 实战 link 缺** (跟"同类症状",配合):
- KALLAX Onramp v1.3.0 + v2.0.0 体系架构都依赖 skill 触发, 但 skill 永远不 surface, **整个体系失效**
- 23 Rule + 5 扩展组 + 2 Onramp 命令 + 3 docs/段 全部基于"skill 能被 trigger"的假设, 假设**假**
- "对齐一下流程" v2.0.0 的 §2 流程 + A+B review 实战全部受这影响 (跟"同类症状",配合)

---

## 2. 设计原则 (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

| # | 原则 | 跟"同类症状",配合 |
|---|---|---|
| 1 | **加 YAML frontmatter 跟 89 skills 对齐** (跟"同类症状",配合) | ✅ 跟"诚实修正评估",配合 |
| 2 | **mirror 31 slash command 到 .opencode/command/** 跨平台 (跟"同类症状",配合) | ✅ 跟"独立" 拍 explicit 约束,配合 |
| 3 | **0 增 Rule** (跟 Rule 32 软约束升级阈值,配合) | ✅ 跟"流程逻辑" 战略 一致 |
| 4 | **0 重写** (跟 Rule 5 DRY,配合) | ✅ 跟"翻篇&精进" 战略 一致 |
| 5 | **走对策 A+B+C** 落地 (跟"同类症状",配合) | ✅ 跟 Rule 11/14/15,配合 |

---

## 3. 实施 (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

### 3.1 Task 1: 加 YAML frontmatter (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Files:**
- Modify: `~/.claude/skills/kallax/SKILL.md` (1 line prepended)

**Step 1.1**: 写 frontmatter 跟 89 skills 对齐, 跟"同类症状",配合, 跟"独立" 拍 explicit 约束,配合:

```yaml
---
name: kallax
description: Use when user types `/kallax-panel`, `/kallax-expert`, `/kallax-skill`, `/kallax-init`, `/kallax-takeover`, or mentions "expert panel", "architecture review", "kallax expert", "召唤专家", "专家评审", "kallax 多 agent", "subagent workflow", "EPIC 拆解", "PHASE review", "BE 教训", or invokes a multi-role expert review for a new EPIC, architectural decision, or major refactor. Spawns a 5-person panel (Architect, Backend, Frontend, UX, Product) + 5 extended (security-tool-bypass + process-engineering + auditor + compliance + decision-gate). Do NOT trigger for simple bug fixes, single-domain tasks, or chores.
triggerKeywords: [kallax, expert panel, architecture review, 召唤专家, 专家评审, multi-agent, subagent, EPIC 拆解, PHASE review, 实战, 反思, BE 教训, A+B review, 5 扩展组, 5 视角, 决策疲劳, KALLAX Onramp, 跟"同类症状" 完整完成, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合, 跟"反哺框架" 战略 一致, 跟"翻篇&精进" 战略 一致, 跟"流程逻辑 > 扩充配置" 战略 一致]
filePath: ~/.claude/skills/kallax/SKILL.md
---

# KALLAX Skills 命令索引
...
```

**Step 1.2**: commit + push (跟"同类症状",配合, 跟"诚实修正评估",配合).

### 3.2 Task 2: 修 KALLAX 项目内 5 default skill 文档 (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Files:**
- Create: `.claude/skills/kallax/default/{architect,backend,frontend,ux,product}.md`

**Step 2.1**: 5 default skill 文档**当前不存在** (实测显示). 跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合:
- KALLAX v1.2.4 5 default skill 文档 拍板 落地, 实际 v2.0.0 release 没带 (跟 BE-15 形式通过实质失败 模式 一致)
- 配合 v1.2.4 release 模式 一致, 跟 5 扩展组 skill 文档 (在 `extended/`) 跟"同类症状",配合

**Step 2.2**: 写 5 default skill 文档 (跟 KALLAX-GLOSSARY.md 模式 一致, 跟 5 extended skill 文档 模式 一致, 跟"同类症状",配合).

### 3.3 Task 3: mirror 31 slash command 到 .opencode/command/ (跟"同类症状",配合, 跟"诚实修正评估",配合)

**Files:**
- Create: `<project>/.opencode/command/<name>.md` × 31

**Step 3.1**: 跟 31 `~/.claude/commands/kallax-*.sh` 一致, mirror 到 `<project>/.opencode/command/kallax-*.md`:
- 每个 mirror 是 markdown 文件, 跟 frontmatter `description:` + `prompt:`
- 跟"同类症状",配合, 跟"独立" 拍 explicit 约束,配合

### 3.4 Task 4: 升 v2.0.2 release (跟"同类症状",配合, 跟"翻篇&精进" 战略 一致)

**Files:**
- Modify: `package.json` (2.0.1 → 2.0.2)
- Modify: `CHANGELOG.md` (append v2.0.2 segment)

**Step 4.1**: 升 package.json + commit + push + merge miao + tag v2.0.2.

---

## 4. Self-Review (跟 Rule 9,配合, 跟"同类症状" 完整完成, 跟"诚实修正评估",配合)

**1. Spec coverage**: 4 task 全部覆盖
- §1.1 frontmatter → T1
- §1.3 5 default skill 缺 → T2
- §1.2 跨平台 mirror → T3
- v2.0.2 release → T4

**2. Placeholder scan**: 0 个 TBD

**3. Type consistency**: description + triggerKeywords 跟 89 other skills 一致

**4. Ambiguity**: 0 ambiguous

---

## 5. Execution Handoff (跟"同类症状",配合, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合)

Spec written to `docs/superpowers/specs/2026-06-16-kallax-v2.0.2-skill-frontmatter-design.md`.

**Subagent-Driven** (推荐) - 派 1 Performer subagent 走 4 task, 推 v2.0.2

---

**跟决策者"全部采纳" explicit 授权,配合, 跟"同类症状" 完整完成, 跟"诚实修正评估",配合, 跟"独立" 拍 explicit 约束,配合, 跟"流程逻辑 > 扩充配置" 战略 一致, 跟"翻篇&精进" 战略 一致, 跟 13 release 累计,配合, 跟 23 Rule 累计,配合, 跟 5 default + 5 extended 累计,配合, 跟 89 skills 对齐**
