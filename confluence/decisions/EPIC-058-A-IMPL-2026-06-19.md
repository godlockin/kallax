# EPIC-058-A — 派遣 task 描述 跟 现状 mismatch 揭示: 跨 release 留待 治理 gap

> 跟 v2.0.3 EPIC-056-A 联合 | 跟"反哺框架" 战略 联合 | Date: 2026-06-25 (重写) | 跟"诚实修正" 战略 联合 0 简单 记录

## 1. 复利 价值 (跟 未来 影响 联合)

**对 未来 复利 作用 (无论 正向 反向)**:

- **正向**: 4-Level Fact-Forcing Level 1 (Existence) 优先 → 任何 subagent 派遣 1 步 先 verify task 描述 跟 现状 一致, 0 no-op edit, 0 cross-cutting change. 跨 release 累计 适用 — 任何 派遣 task 描述 跟 现状 失一致 都用此模式.

- **反向**: `.git/hooks/pre-commit` 跟 `scripts/hooks/pre-commit` 不同步 (缺 93 lines v2.7.0+ 加的 Check 2.6/3) → 任何 subagent 走 pre-commit hook 0 命中 Check 2.6/3, 7 anti-pattern 硬性 校验 0 触发, 反复 风险.

**核心 insight**: 派遣 task 描述 基于 假设, 不 基于 现状 → 治根 模式 = 派遣 first verify → 跨 release 适用, 1 ticket 1 subagent 串行 100% 实施.

## 2. 反讽 (跟 治理 gap 联合)

`scripts/hooks/pre-commit:215` 跟 `.git/hooks/pre-commit:187` 双 路径 都已 含 `^jira/` (跟 commit `e9b56aa` v2.3.0 加的 联合). 派遣 task 描述 "缺 `^jira/`" 跟 现状 失一致, 0 NEW ALLOWED_PATTERNS entry 必要.

**反讽 模式**: 治根 `--no-verify` workaround 的 EPIC-058-A, 如 用 `--no-verify` 强行 commit, 跟"反讽" 联合 (治的 跟 用的 是同一个). 跟 v2.7.4 整理 release 联合, 跟"不冒进" 战略 联合 0 冒进.

## 3. 治理 gap 暴露 (跟"独立" 战略 联合 0 跨 session 拍)

- **gap-1**: `.git/hooks/pre-commit` 跟 `scripts/hooks/pre-commit` 不同步 93 lines (跟 install.sh 不 1:1 sync) → 任何 install 治根 反复, 跨 release 留待
- **gap-2**: conductor role 不能 miao.write, 0 跨 session 拍板 → 任何 subagent 跨 release 留待 master token
- **gap-3**: ALLOWED_PATTERNS array 扩 治根 反复 (跟 v2.7.4 B2 联合) → 跨 release 累计 `^scripts/`, `^tests/`, `^web/`, `^confluence/` 等 加, 0 文档化 扩 模板

## 4. 实际 deliver (跟"翻篇&精进" 战略 联合 0 简单 记录)

- ✅ sync `.git/hooks/pre-commit` 跟 `scripts/hooks/pre-commit` 1:1 (325 lines both, 含 Check 2.6/3 v2.7.0+ 加的部分)
- ✅ 加 test `tests/integration/pre-commit-allow-jira-test.sh` (11/11 PASS, 7 mock + 4 集成, 0 false positive)
- ✅ 0 增 Rule, 0 增 命令, 0 增 ALLOWED_PATTERNS entry (跟 派遣 §"0 增" 联合, 0 跨 release 留待)
- ⚠️ commit BLOCKED-on-master-拍板 (跟"不冒进" 联合, 不 用 `--no-verify` workaround)

## 5. 跟 ... 联合 (3-5 行, 0 重复 KPI)

- 跟 EPIC-058-B/C/D 0 file overlap 联合, file scope 1 ticket 隔离
- 跟 v1.0.6 EPIC-051 联合, 验证 `^jira/` 治根 仍然 有效, 0 false positive
- 跟 v2.7.4 B1-3 治根 联合, 0 NEW errors 引入 (2 pre-existing WARNINGS 跟现状 一致)
- 跟 EPIC-059-D Fact-Forcing 联合, 0 假 PASS 校验 100% (跟 4-Level Fact-Forcing Level 1 揭示 联合)
- 跟 8acdb1a 9b6bc91 跟 EPIC-060-A/B/C 74f48af 跨 release 累计 联合, 跟"反哺框架" 战略 1 拍 explicit 拍板
