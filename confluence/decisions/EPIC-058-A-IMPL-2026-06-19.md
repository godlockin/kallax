# EPIC-058-A-IMPL-2026-06-19 — pre-commit `^jira/` ALLOWED_PATTERNS 验证 + .git/hooks 同步 治根

> **跟 v1.0.6 EPIC-051 联合, 跟 v2.7.0 整理 release 联合, 跟 v2.7.4 B1-3 治根 联合, 跟 EPIC-059-D Fact-Forcing 联合, 跟"翻篇&精进" 战略 一致, 跟"诚实修正" 联合**
> **跟 KALLAX-GLOSSARY §11.5 "不冒进" + §11.1 "DRY" + §11.4 "单一真相" 联合**

**Date**: 2026-06-19
**Author**: performer-EPIC-058-A (KALLAX Subagent 1/8)
**Reviewers**: Conductor (4-Level verify) + 主公 (战略审批)
**Status**: ⚠️ BLOCKED-on-commit — Deliver 完整 (sync + test + doc) 但 commit 需 master role 拍板
**Scope**: 1 sync (cp), 1 test (new, 11/11 PASS), 1 doc (this), 0 Rule 增, 0 命令 增, 0 ALLOWED_PATTERNS array change
**Tickets**: EPIC-058-A (1/1 deliver done, commit 待 master 拍板, 跟 EPIC-058-B 0 file overlap 联合)

---

## TL;DR

派遣 task 描述基于"现状: 13 patterns, 缺 `^jira/`" 假设, 但 **4-Level Fact-Forcing Level 1 (Existence) 验证** 揭示:

- ✅ `^jira/` 已在 `scripts/hooks/pre-commit:215` (commit `e9b56aa` v2.3.0 加, 跟 P1-1 EPIC-058 deferred ticket 联合)
- ✅ `^jira/` 已在 `.git/hooks/pre-commit:187`
- ❌ **真实差异**: `.git/hooks/pre-commit` 缺 v2.7.0+ 加的 Check 2.6 (anti-patterns WARN) + Check 3 (build artifacts 防御) — **out of sync 93 lines**

按"翻篇&精进" 战略 + "诚实修正" + 9 Hard Rules (Rule 9: 0 cross-cutting changes) 联合:
- **不**做 no-op edit (加已存在的 pattern = cross-cutting noise)
- **治根 deliver** = sync 实际差异 + test 覆盖验证 + 实施报告
- **0 增 Rule 0 增命令 0 增 ALLOWED_PATTERNS entry** (跟 task 派遣 §"0 增 Rule 0 增命令" 联合)
- ⚠️ **commit BLOCKED** — pre-commit hook fail-closed: role=conductor 不允许 miao.write, 需 master 拍板 (跟"不冒进" 联合, 不 用 --no-verify workaround)

---

## 1. 现状 (跟 task 描述 联合)

### 1.1 `^jira/` pattern 存在性 验证

```bash
$ grep -n "'\\^jira/'" scripts/hooks/pre-commit .git/hooks/pre-commit
scripts/hooks/pre-commit:215:  '^jira/'                        # v2.3.0 — jira tickets/epics direct commits
.git/hooks/pre-commit:187:  '^jira/'                        # v2.3.0 — jira tickets/epics direct commits
```

**结论**: 派遣 task 描述的"缺 `^jira/`" 跟 现状 不一致, 现状 **14 patterns** (含 `^jira/`, 跟 commit e9b56aa v2.3.0 一致)。

### 1.2 `.git/hooks/pre-commit` sync 现状

```bash
$ wc -l scripts/hooks/pre-commit .git/hooks/pre-commit
     325 scripts/hooks/pre-commit
     232 .git/hooks/pre-commit
$ diff -q scripts/hooks/pre-commit .git/hooks/pre-commit
Files scripts/hooks/pre-commit and .git/hooks/pre-commit differ
```

**真实差异**: `.git/hooks/pre-commit` 缺 93 lines (Check 2.6 + Check 3 v2.7.0+ 加的部分):
- Check 2.6: Anti-Pattern Check (WARN 模式, 7 硬性 约束)
- Check 3: Build artifacts 防御 (rust/target/, node_modules/, dist/, .o/.so/.dylib 等)

### 1.3 治根 策略 (跟"翻篇&精进" + "诚实修正" 联合)

按 9 Hard Rules (Rule 9: 0 cross-cutting changes) + 派遣 §"0 增 Rule 0 增命令" 联合, 治根 deliver:
1. **不**做 no-op edit (加已存在的 `^jira/` pattern = 0 价值 cross-cutting change)
2. **sync** `.git/hooks/pre-commit` 跟 `scripts/hooks/pre-commit` 一致 (治根 Check 2.6/3 部署)
3. **加 test** `tests/integration/pre-commit-allow-jira-test.sh` (7 mock + 4 集成 = 11/11 PASS) — 验证 `^jira/` 命中 后 0 false positive
4. **commit** 含 test (新) + sync (gitignored, 跟 install.sh 一致) — **BLOCKED-on-master-拍板**
5. **0 Rule 增 0 命令增 0 ALLOWED_PATTERNS array entry 增** (跟 task 约束 联合)

---

## 2. 实施 步骤 (跟 task 派遣 § 联合)

### 2.1 步骤 0: 4-Level Fact-Forcing Level 1 验证 (Existence)

```bash
$ grep -n "'\\^jira/'" scripts/hooks/pre-commit .git/hooks/pre-commit
scripts/hooks/pre-commit:215:  '^jira/'                        # v2.3.0 — jira tickets/epics direct commits
.git/hooks/pre-commit:187:  '^jira/'                        # v2.3.0 — jira tickets/epics direct commits
```

✅ **Existence verified** — `^jira/` 已在两边 (scripts/hooks + .git/hooks)

### 2.2 步骤 1-2: 跳过 (按 9 Hard Rules Rule 9 联合, no-op edit 跳过)

**不**执行 task 派遣 step 3 (加 `^jira/`), 现状已含。

### 2.3 步骤 3: sync `.git/hooks/pre-commit` (task 派遣 step 4/5 联合)

```bash
$ cp scripts/hooks/pre-commit .git/hooks/pre-commit
$ chmod +x .git/hooks/pre-commit
$ diff -q scripts/hooks/pre-commit .git/hooks/pre-commit
$ echo "EXIT=$?"
EXIT=0
$ grep -n "'\\^jira/'" .git/hooks/pre-commit
215:  '^jira/'                        # v2.3.0 — jira tickets/epics direct commits
```

✅ **Sync OK** — `.git/hooks/pre-commit` == `scripts/hooks/pre-commit` (325 lines both, 含 Check 2.6 + Check 3 v2.7.0+ 加的部分)

### 2.4 步骤 4: 加 test `tests/integration/pre-commit-allow-jira-test.sh` (task 派遣 step 6 联合)

7 mock scenarios + 4 集成 tests = 11/11 PASS (100.0%):

| # | Scenario | Expected | Result |
|---|----------|----------|--------|
| TC1 | jira/tickets/ + jira/epics/ | ALLOWED | ✅ PASS |
| TC2 | jira/schemas/ | ALLOWED | ✅ PASS |
| TC3 | jira/phases/ | ALLOWED | ✅ PASS |
| TC4 | jira/_archive/, jira/_archived/, jira/epic_index.json | ALLOWED | ✅ PASS |
| TC5 | jira + other allowed 混 | ALLOWED | ✅ PASS |
| TC6 | src/jira.rs (false positive 防御) | BLOCKED | ✅ PASS |
| TC7 | scripts/jira/foo.sh (不 是 jira/ dir) | BLOCKED | ✅ PASS |
| 集成 1 | scripts/hooks/pre-commit 含 `^jira/` pattern | TRUE | ✅ PASS |
| 集成 2 | `.git/hooks/pre-commit` == `scripts/hooks/pre-commit` (sync) | TRUE | ✅ PASS |
| 集成 3 | `.git/hooks/pre-commit` 含 `^jira/` pattern | TRUE | ✅ PASS |
| 集成 4 | `bash -n scripts/hooks/pre-commit` syntax | OK | ✅ PASS |

**Summary**: 11/11 PASS (100.0%)

### 2.5 步骤 5-7: 验证 (task 派遣 step 7-9 联合)

```bash
$ bash -n scripts/hooks/pre-commit
EXIT=0  # Syntax OK
```

```bash
$ bash tests/integration/pre-commit-allow-jira-test.sh
... 11/11 PASS, EXIT=0
```

```bash
$ bash scripts/check-anti-patterns.sh | tail -3
[WARN] Anti-Pattern Check: 0 ERRORS, 2 WARNINGS
# 2 WARNINGS pre-existing (硬编码 /Users/ in docs, console.log in node/src/),
# 0 ERRORS = 0 NEW errors 引入
```

✅ **0 NEW errors 引入** (2 pre-existing WARNINGS 跟现状 一致, 不 在本 EPIC scope)

### 2.6 步骤 8: Commit BLOCKED (跟"不冒进" 联合)

```bash
$ git commit -m "..." 2>&1
DENIED: miao.write for role conductor (actor: Steven Chen)
║  BLOCKED: Authorization denied by authz check. ║
║  Action: miao.write                                        ║
║  Actor: Steven Chen                                        ║
```

**原因**:
- pre-commit hook fail-closed (跟 v2.7.4 "0 埋坑" 战略 一致)
- 当前 role = `conductor` (从 `.kallax/state/state.json`)
- conductor 权限矩阵 不含 `miao.write` (只 master 可 miao.write, 跟 check.sh:103-114 联合)
- `--no-verify` 正是 EPIC-058-A 要治根的 workaround (反讽! 治根的 ticket 用 workaround, 跟"反讽" 联合)

**决策**: 按 "不冒进" 战略 + "诚实修正" 联合, 不 用 `--no-verify` 强行 commit. 报告 BLOCKED-on-master-拍板, 等主公 (master role) 拍板 commit.

**注**: 之前 commit `master_main <master@kallax.local>` (跟 git log 联合) 都是 master role 拍的, 跟 conductor subagent 不同. subagent commit 需要 master 拍板.

---

## 3. 跟 相关 EPIC 联合

### 3.1 跟 v1.0.6 EPIC-051 联合 (历史 ticket)

EPIC-051 记录 jira/ 下 file 改 触发 `git add` 误判 跟 `--no-verify` workaround 问题. 治根 = `^jira/` 加到 ALLOWED_PATTERNS, 跟 commit `e9b56aa` v2.3.0 一致 (2026-04 加的). EPIC-058-A 验证 此治根 仍然 有效, 0 false positive.

### 3.2 跟 v2.7.0 整理 release 联合

`.git/hooks/pre-commit` sync 治根 Check 2.6 (anti-patterns WARN) + Check 3 (build artifacts 防御) v2.7.0+ 加的 防御. 跟"反讽" + "诚实修正" 联合.

### 3.3 跟 v2.7.4 B1-3 治根 累计 联合

sync + test 模式 跟 v2.7.4 7 anti-pattern 硬性 校验 联合, 0 NEW errors 引入 验证.

### 3.4 跟 EPIC-058-B 0 重叠 联合

EPIC-058-B = worktree/branch 复盘 0 stale (301 lines doc, 0 cleanup 行动). EPIC-058-A = pre-commit 验证 (sync + test + doc, 0 ALLOWED_PATTERNS change). 2 ticket 0 file overlap, 0 commit overlap (独立 commit).

### 3.5 跟"翻篇&精进" 战略 一致

- **0 增 Rule** (跟 v2.4.1 还原 22 Rule 联合)
- **0 增 命令** (跟 0 增 Rule 持平)
- **0 增 ALLOWED_PATTERNS entry** (跟 派遣 task 描述 mismatch, 现状 已含)
- **0 增 cross-cutting change** (跟 9 Hard Rules Rule 9 联合)

### 3.6 跟"不冒进" 联合 (commit BLOCKED 决策)

按 KALLAX-GLOSSARY §11.5 "不冒进", 不为了 显 commit KPI 用 `--no-verify` workaround (这正是 EPIC-058-A 要治根的). 报告 blocked, 等 master 拍板.

---

## 4. KPI 累计 (跟 Rule 9 X/Y 格式 联合)

| KPI | 现状 | 增量 | 累计 |
|-----|------|------|------|
| `^jira/` ALLOWED_PATTERNS 命中 | 14/14 patterns (含) | 0 (no-op) | 14/14 (100.0%) |
| `^jira/` 误判 (false positive) | 0/0 (TC6/TC7 BLOCKED 正确) | 0 | 0/0 (100.0%) |
| `.git/hooks/pre-commit` 同步 | 0/1 (out of sync) | 1/1 (sync) | 1/1 (100.0%) |
| `tests/integration/pre-commit-allow-jira-test.sh` | 0/0 (新) | 11/11 PASS | 11/11 (100.0%) |
| Anti-patterns NEW errors | 0/0 | 0 | 0/0 (100.0%) |
| Commit | 0/1 (待 master 拍板) | 0 (blocked) | 0/1 (待 master) |

---

## 5. 交付清单 (跟 task 派遣 § 联合)

| # | 交付 | 状态 | 引用 |
|---|------|------|------|
| 1 | scripts/hooks/pre-commit (no-op) | ✅ 14 patterns 已含 `^jira/` | line 215 |
| 2 | .git/hooks/pre-commit (sync) | ✅ 跟 scripts/hooks/pre-commit 一致 (325 lines) | cp after diff |
| 3 | tests/integration/pre-commit-allow-jira-test.sh (新) | ✅ 11/11 PASS | new file |
| 4 | confluence/decisions/EPIC-058-A-IMPL-2026-06-19.md (本 doc) | ✅ 1-2 页 | this file |
| 5 | commit | ⚠️ BLOCKED-on-master-拍板 | 待 master 拍板 |
| 6 | 0 增 Rule 0 增 命令 | ✅ 0/0 | n/a |

---

## 6. 教训 (跟"诚实修正" 联合)

### 6.1 派遣 task 描述 跟 现状 mismatch

task 描述基于"13 patterns 缺 `^jira/`" 假设, 但 现状 已含 (14 patterns, commit e9b56aa v2.3.0 加的). 按 4-Level Fact-Forcing Level 1 (Existence) 优先 — **不**做 no-op edit.

### 6.2 真正 deliver = sync + test

- **sync** 治根 `.git/hooks/pre-commit` 跟 `scripts/hooks/pre-commit` 一致 (缺 93 lines v2.7.0+ 加的部分)
- **test** 覆盖 7 mock + 4 集成 = 11/11 PASS, 0 false positive (TC6/TC7 BLOCKED 正确)
- **doc** 诚实记录 "0 NEW code change" 跟 "sync 治根" 跟"翻篇&精进" 战略 一致

### 6.3 commit BLOCKED 跟"不冒进" 联合

conductor role 不能 miao.write, `--no-verify` 是治根目标. 不 强行 commit, 报告 blocked, 等 master 拍板 — 跟"诚实修正" 联合, 不 埋坑.

### 6.4 跟"反讽" 联合

治根 `--no-verify` workaround 的 EPIC-058-A, 如 用 `--no-verify` 强行 commit, 跟"反讽" 联合 (治的 跟 用的 是同一个). 跟 v2.7.4 整理 release 联合.

---

## 7. 后续行动 (待 master 拍板)

按"翻篇&精进" 战略 + "诚实修正" 联合:
- **1 commit 必要** (master 拍板): `git add tests/integration/pre-commit-allow-jira-test.sh confluence/decisions/EPIC-058-A-IMPL-2026-06-19.md && git commit -m "..."`
- **0 cleanup 行动 必要** (现状 14 patterns 已含 `^jira/`, 0 false positive, sync 治根 done)
- **0 复盘 follow-up 必要** (跟 EPIC-058-B 0 stale 复盘 闭环 模式 一致)
- **1 复盘 doc 落地** (本 doc, 跟 EPIC-058-B 闭环 模式 一致)

---

**Status**: ⚠️ BLOCKED-on-commit — Deliver 完整 (sync + test + doc), commit 待 master 拍板
**跟 v1.0.6 EPIC-051 联合**: 验证 `^jira/` 治根 仍然 有效, 0 false positive, 0 `--no-verify` workaround 必要 (但 commit 也 不用)
**跟 v2.7.4 整理 release 联合**: 0 NEW errors 引入, 0 Rule 增 0 命令 增 0 ALLOWED_PATTERNS entry 增
**跟"翻篇&精进" 战略 一致**: 0 增 Rule, 0 增 命令, 0 增 cross-cutting change
**跟"不冒进" + "诚实修正" 联合**: commit BLOCKED, 不用 `--no-verify` workaround, 等 master 拍板
