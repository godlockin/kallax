# EPIC-202-A: 工具代码修 — 拍板记录 (2026-08-07)

> **起源**: 4 专家挑刺 EPIC-197/199/200/201 后挑出的 4 CRITICAL + 2 MAJOR + 1 MINOR 工具代码问题。
> **对抗式**: 修完后派 Tooling 专家回来挑刺 (SendMessage resume), 0 CRITICAL 才进 4-PR。

## 修复清单

| # | Severity | 问题 | 修法 |
|---|----------|------|------|
| 1 | CRITICAL | fix-stale-links link text 含括号 `()` 时 `newLine.replace` 静默失败 | 改用 match.index + length 区间替换, 不依赖 text replace |
| 2 | CRITICAL | `#?tab=active` anchor/query 切割错误 | ref.replace 改 `/[?#].*$/` (包含 `?` query) |
| 3 | CRITICAL | epic-200/201 test shell `awk '{print $2}'` 在 JSON 输出上假 PASS | 改用 `jq -r '.stale_refs // empty'` |
| 4 | CRITICAL | post-process.sh 注释 fail-soft, 实际 fail-fast | 注释里明确"set -euo pipefail + || true 兜底" |
| 5 | MAJOR | check-internal-refs 漏 web/ HTML 引用 | 默认收集 `web/` scope |
| 6 | MAJOR | check-internal-refs 漏 root `.json`/`.html` | 根目录 regex 扩 `\.(md|txt|json|html)$` |
| 7 | MAJOR | fix-stale-links 无 `--dry-run` / `--help` | 加两个 flag + help 输出 |

## 修复中发现的新问题 (后续 fix)

| # | 问题 | 修法 |
|---|------|------|
| 8 | HTML ref 期望相对 REPO_ROOT, 不是相对 srcDir | isHtmlOrJson 特判, ref normalize 后去 `../` 前缀 |
| 9 | `path.join` 把 `../` 立刻解析, 不保留 | 手动 normalize `../` 链, 不用 path.resolve |
| 10 | `match[1] || match[2]` 误把 link text 当 path | 改 `match[2] || match[1]` (path 优先) |
| 11 | fix-stale-links 不扫 HTML/JSON | 加 htmlRefPattern + jsonRefPattern 1:1 兼容 check-internal-refs |
| 12 | EPIC-201 拍板记录里 markdown 占位符被误报 | 改 inline code 包装 |

## 验证

- `node scripts/check-internal-refs.cjs` → **98 refs, 0 stale** (含 web scope)
- `node scripts/fix-stale-links.cjs` → 8 stale fixed in 1 file (web/showcase/index.html)
- `bash tests/integration/epic-200-doc-audit-test.sh` → **21 PASS, 0 FAIL**
- `bash tests/integration/epic-201-audit-extension-test.sh` → **7 PASS, 0 FAIL**

## 对抗式 review 待办

派原 Tooling 专家 (afc9e1ce822178822) 回来挑刺本 EPIC-202-A, 确认 0 CRITICAL 才进 4-PR。

---

Co-Authored-By: Claude <noreply@anthropic.com>