# EPIC-168-BG Decision Record

**Date**: 2026-08-05
**Phase**: 5G (北极星 dashboard 闭环 EPIC-023-C) + 5B (daemon 强化)
**Status**: Implemented

## Decision

合并 Phase 5 G (北极星 dashboard 闭环) 和 Phase 5 B (daemon 强化) into EPIC-168-BG.

## Rationale

1. **Dashboard 闭环**: EPIC-023-C 北极星 dashboard 需要 `dashboard-metrics.sh` + `web/dashboard-metrics.html`
2. **Daemon 强化**: EPIC-166 daemon 有 4 个 bug 需要修:
   - Bug 1: quota 调用缺 ticket_id
   - Bug 2: scheduler 4 priority 全返回 P2
   - Bug 3+4: run-history emit jq 失败 + 无 flock

## Changes

### Bug Fixes

| Bug | File | Issue | Fix |
|-----|------|-------|-----|
| Bug 1 | heartbeat-daemon.sh:104 | quota 调用无 ticket_id | 加 `resolve_active_ticket()` 找 in_progress ticket |
| Bug 2 | scheduler-hint.sh | 4 priority 全返回 P2 | daemon 传 ticket_id 而非完整 quota 输出 |
| Bug 3 | run-history.sh | jq --argjson 失败 | 改直接字符串拼接 |
| Bug 4 | run-history.sh | 无 flock | 加 `flock -x` 保护 append |

### New Files

| File | Purpose |
|-------|---------|
| `scripts/dashboard/dashboard-metrics.sh` | 4 北极星 + 4 event counts 聚合 |
| `web/dashboard-metrics.html` | 静态 HTML dashboard (vanilla JS) |
| `tests/integration/dashboard-metrics.test.sh` | 7-case dashboard 测试 |

### Test Results

```
heartbeat-daemon-runtime.test.sh: 8/8 PASS
dashboard-metrics.test.sh: 12/12 PASS
60s daemon run: 0 failed lines in log
```

## Raw Output

See:
- `/tmp/claude-tasks/heartbeat-test-*.log`
- `/tmp/claude-tasks/dashboard-test-*.log`
