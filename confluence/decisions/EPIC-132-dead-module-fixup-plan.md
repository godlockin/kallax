# EPIC-132 Dead-Module Fixup — Plan

> **起源**: 主公 2026-07-20 命令 "3 在 testing 分支做"。
> **目标**: 处理 miao tip `9c12ebe` 上 sentinel 抓到的 29 unique dead modules (重复扫到 90 处因 import 多路径)。
> **执行位置**: testing 分支 (跟 miao 隔离, 不阻塞 prod)

## Dead modules baseline (2026-07-20 from miao tip 9c12ebe via scripts/scan-dead-code.sh)

```
core/brief-inference/quality
core/brief-inference/assignment
core/brief-inference/types
core/brief-inference/claim-gate
core/data-adapter/helpers
core/data-adapter/types
core/data-adapter/file-adapter
core/data-adapter/sqlite-adapter
core/schema-validator
core/context/budget-manager
core/context/auto-compressor
core/context/restore
core/context/extractor
core/context/archiver
core/master-verify/helpers
core/master-verify/constants
core/dag-visualizer
core/sqlite/sync-client
core/sqlite/types
core/sqlite/task-ops
core/sqlite/trace-ops
core/sqlite/instance-message-ops
core/sqlite/ticket-ops
core/recommender/scorer
core/recommender/matcher
core/master-verify-bridge
core/redis-pubsub
core/expert-invocations-queue/redis-backend
... (29 unique)
```

外加 sentinel 抓到的其他债:

| 类型 | 位置 | 修法 |
|------|------|------|
| `@ts-ignore` 残留 | `node/src/types/index.ts:3` (注释), `node/src/core/plugin-system.ts:4` (注释) | 都是 doc-string 内, 确认无 active `@ts-ignore` directive |
| `any` 残留 | 多处 (allowed WARN) | 主公 CLAUDE.md Rule 1 warn, 不强制 fail |
| TODO/FIXME | `types/index.ts:131`, `output-verifier.ts:304/305` 等 | review logic 后清理 |
| catch (e: any) | 多处 | 改 `e: unknown` + 类型 guard |

## 计划 (分 PR commit, 4 阶段)

### Phase A — Sanity check sentinel accuracy (1 PR)
- 跑 `bash scripts/scan-dead-code.sh > docs/test-baseline-2026-07-20.txt`
- docs/dead-modules-baseline.md 沉淀 baseline list (跟此 .md 一致)
- **不修任何代码**, 仅取证

### Phase B — Restore imports (1-3 PR)
- 对 dead modules 检查: 是否真的 dead, 还是 import 路径 mismatch (eg. ESM `.js` 后缀)
- 优先: 加 test import 让 sentinel 真覆盖, **不要**删 module (低风险, 不删 dead, 反正显 scope)

### Phase C — `@ts-ignore` cleanup (1 PR)
- 注释 vs active directive 分类
- 删 active `@ts-ignore` (有的话)

### Phase D — Catch unknown type alignment (1 PR)
- 全 grep `catch (e: any)` / `catch (e)` → 改 `catch (e: unknown)` + type guard
- 同步 CLAUDE.md Rule 3

### Phase E — Push to testing, sync miao (1 PR)
- `bash scripts/branch-sync.sh testing` 开 PR testing → miao
- 主公 review + merge

## 反模式 (本次不做)

❌ **不在 testing 分支做**:
- 改 tsconfig (开了 strict 是治根)
- 引 Zod schema (Architect 1.0 推荐, 留独立 EPIC)
- 删 node module (即使"死", 删动态语言特性容易破坏 — 默认保留)

## Sentinel coverage "100% rule" 目标

完成后, `bash scripts/scan-dead-code.sh` 应该:
- Stage 1 @ts-ignore: 0 处
- Stage 2 tsc: 0 errors
- Stage 3 sentinel: **0 / 151 modules uncovered** (或 ≤5 — 留几个 helper module 不需要 test)

## 跟进 ticket

| EPIC | 关联 |
|------|------|
| EPIC-131 | TS strict 33 errors → 0 (前置,已 merge) |
| EPIC-131-B | sentinel system (前置,已 merge) |
| EPIC-132 | **本次** — dead modules 清除 |

## 已知 (诚实)

1. **29 dead modules 不是 29 bug**: 可能是 dynamic import, reflection-based load, plugin-system dynamic load — 需人工逐个判定
2. **不动 merge timing**: 主公说 "3 在 testing 分支做" — testing ↔ miao 同步由 PR + 流程走
3. **不会"删任何 module"**: 主公没说要删, 我保守恢复 test, 不要 reduce surface area

---
🤖 Generated by Sonnet 4.5 on 2026-07-20, planned by EPIC-129 sync governance + EPIC-131-B sentinel
