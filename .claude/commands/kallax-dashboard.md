---
description: Generate KALLAX static dashboard (EPIC-281 Phase 1 MVP).
---

!bash "$(dirname "$0")/../scripts/dashboard-p1/emit.sh" $ARGUMENTS

If $ARGUMENTS is empty, also start the local preview server on 127.0.0.1:8765.